# ⚡ GEAR VAULT 배포 가이드

총 소요 시간: **약 15분**

---

## STEP 1 — Supabase DB 세팅 (5분)

1. [supabase.com](https://supabase.com) 로그인
2. 기존 프로젝트 선택 (또는 New Project 생성)
3. 왼쪽 메뉴 **SQL Editor** 클릭
4. `supabase_setup.sql` 파일 전체 내용 복사 → 붙여넣기 → **Run** 클릭
5. 에러 없이 완료되면 OK ✅

**API 키 복사해두기:**
- 왼쪽 메뉴 **Settings → API**
- `Project URL` 복사
- `anon public` 키 복사
- 둘 다 메모장에 저장해두세요

---

## STEP 2 — GitHub에 코드 올리기 (5분)

### 방법 A: GitHub 웹에서 직접 (코딩 몰라도 OK)

1. [github.com](https://github.com) 로그인
2. 오른쪽 위 **+** → **New repository**
3. Repository name: `gearvault` (public 또는 private 둘 다 OK)
4. **Create repository** 클릭
5. 페이지에서 **"uploading an existing file"** 링크 클릭
6. 이 폴더 전체를 드래그 앤 드롭
7. **Commit changes** 클릭

### 방법 B: 터미널 사용 (개발자 방식)

```bash
cd gearvault
git init
git add .
git commit -m "first commit"
git remote add origin https://github.com/본인계정/gearvault.git
git push -u origin main
```

---

## STEP 3 — Vercel 배포 (5분)

1. [vercel.com](https://vercel.com) 로그인
2. **Add New → Project**
3. GitHub 연결 → `gearvault` 레포지토리 선택 → **Import**
4. **Environment Variables** 섹션에서 아래 2개 추가:

| 키 이름 | 값 |
|---------|-----|
| `VITE_SUPABASE_URL` | Supabase Project URL |
| `VITE_SUPABASE_ANON_KEY` | Supabase anon public 키 |

5. **Deploy** 클릭
6. 1~2분 후 `https://gearvault-xxx.vercel.app` 형태의 URL 생성 🎉

---

## STEP 4 — 도메인 커스텀 (선택, 무료)

Vercel 대시보드 → 프로젝트 → **Settings → Domains**
- 무료 서브도메인 변경: `gearvault.vercel.app` 같은 이름으로 변경 가능

---

## 나중에 코드 수정 후 재배포하는 법

코드를 수정해서 GitHub에 push하면 **Vercel이 자동으로 재배포**해요.
같은 URL 그대로 유지됩니다.

```bash
git add .
git commit -m "수정 내용"
git push
```
→ 끝! 1~2분 후 자동 반영

---

## 문제 해결

**흰 화면만 나올 때**
→ Vercel 대시보드 → Deployments → 빨간 빌드 클릭 → 에러 메시지 확인
→ 대부분 환경변수 오타 문제

**데이터가 안 불러와질 때**
→ Supabase SQL Editor에서 `select * from items;` 실행해서 테이블 확인
→ RLS 정책이 제대로 생성됐는지 확인 (SQL 다시 실행)

---

배포 완료 후 URL을 알려주시면 잘 됐는지 같이 확인해드릴게요! 🚀
